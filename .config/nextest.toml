[profile.default]
slow-timeout = { period = "60s", terminate-after = 2 }

[[profile.default.overrides]]
filter = "package(integ) and test(udfs)"
slow-timeout = { period = "180s", terminate-after = 2 }
