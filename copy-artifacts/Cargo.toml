[package]
name = "copy-artifacts"
version = "0.8.1"
edition = "2021"

[dependencies]
arroyo-storage = { path = "../arroyo-storage" }
regex = "1"
tokio = {version = "1", features = ["fs", "rt", "net"]}
webpki = ">=0.22.2"
